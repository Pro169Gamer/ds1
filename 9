>>create folder-> dockerfile,calulator.java
>> create repo and upload above files
>>jenkins -> freestyle ->git->
>>batch command ->docker build -t java-app ->docker build -d -t java-app
->build it 

pipeline {
    agent any

    stages {

        stage('Build Docker Image') {
            steps {
                bat 'docker build -t java-calc-app .'
            }
        }

        stage('Run Container') {
            steps {
                bat 'docker run  java-calc-app'
            }
        }

    }
}
